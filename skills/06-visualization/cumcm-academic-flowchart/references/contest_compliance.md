# 竞赛离线与合规

`--contest-mode` 开启后：

- 禁止联网、在线渲染与第三方 API；
- 禁止搜索 GitHub、CSDN、知乎等平台的当届赛题或答案；
- 只读用户明确提供的本地文件；
- 不输出姓名、学校、赛区、账号或绝对路径；
- 标记 AI 推断内容并记录 AI 操作；
- 提醒用户复核数学逻辑；
- 不保证获奖，不伪造数据、指标或实验结果。

核心 draw.io、SVG 和 PNG 生成必须离线运行。外部适配器不得成为核心依赖。竞赛规则、学校规定和人工审核优先于自动化便利。
